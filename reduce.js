const numbers = [10, 20,];
let temp = 0;
numbers.map((numbers) => 
    temp = temp + numbers
);
console.log(temp)


// Reduce Mathods -->
const numBers = [10,10,20]
const reduce = numBers.reduce((acc,currValue)=> acc*currValue,1)
console.log(reduce)

// Terminal Result  -->2000
